# Old Codex archive

This directory holds the previous implementation (extension in React/TypeScript/WXT,
landing page, admin panel, and Supabase backend) exactly as it stood before the
ground-up rebuild started on 2026-07-16.

It is kept for reference only — nothing here is built, deployed, or maintained
going forward. The rebuild starts fresh from `tampermonkey.js` (gitignored, kept
locally) and `docs/handoff/PROMPT_MESTRE_RECONSTRUCAO_TOTAL.md`, without reusing
any component, service, or schema from this archive.

A tag `pre-rebuild-archive` also marks the exact commit this was moved from, in
case anything here needs to be recovered later.
